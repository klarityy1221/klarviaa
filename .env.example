
# Environment configuration example
# Set NODE_ENV=production in production deployments (e.g. Render) and leave as development locally
NODE_ENV=development

# Local development client URL(s), comma-separated if you use multiple ports
# Example: http://localhost:5173,http://localhost:3000
CLIENT_URL_DEV=http://localhost:5173

# Production client URL(s) (comma-separated if needed)
# Example: https://app.yourdomain.com
CLIENT_URL_PROD=

# Server port
PORT=4000

# Postgres connection (required)
# Example: postgres://user:pass@host:5432/dbname
DATABASE_URL=

# OpenAI API key (required for AI features)
OPENAI_API_KEY=

# Optional: choose a default model for chat (adjust as needed)
OPENAI_MODEL=gpt-4o-mini
JSON_BODY_LIMIT=16kb
# Maximum allowed chat message length (characters)
MAX_MESSAGE_LENGTH=2000

